Drop your featured-insight banner image here, e.g.:

  featured-report.jpg

Then open src/components/sections/FeaturedInsight.tsx and set:

  const IMAGE_SRC = "/images/insights/featured-report.jpg";

(or paste a full https:// URL instead). Until set, a designed placeholder
is shown instead of a broken image.
