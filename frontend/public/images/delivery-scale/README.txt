Put your 5 "Delivery at Enterprise Scale" images here, named exactly:

  workforce.png
  reach.png
  savings.png
  sustainability.png
  spend.png

(PNG with transparent background works best, but jpg/webp will also work —
just update the extension in src/data/content.ts -> scaleStats -> image.)

If a file is missing or fails to load, that card automatically falls back
to the built-in illustrated SVG icon instead of showing a broken image —
so it's always safe to leave one out.
